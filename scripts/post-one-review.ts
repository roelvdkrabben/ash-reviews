/**
 * Post a single approved review
 * 
 * NOTE: This script was for WooCommerce integration which is no longer used.
 * The app now uses Lightspeed API for posting reviews.
 * See /api/cron/post-reviews for the current implementation.
 */

console.log('⚠️  This script is deprecated.');
console.log('The app now uses Lightspeed API for posting reviews.');
console.log('Use the POST /api/cron/post-reviews endpoint instead.');
process.exit(0);
